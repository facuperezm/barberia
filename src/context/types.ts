export interface FormErrors {
  [key: string]: string | undefined;
}

export enum ReservateRoutes {
  FIRST = "/reservate/one",
  SECOND = "/reservate/two",
  THIRD = "/reservate/three",
  REVIEW = "/reservate/review",
}
